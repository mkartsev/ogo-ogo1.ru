extends ../layouts/default

append vars
  -
    page.name  = "personal-addresses"
    page.title = "Адреса"
    //- page.showHeader = true
    //- page.showTitle = true
    //- page.showBreadcrumbs = true

prepend scripts
  //- Скрипты только для этой страницы

block content
  section.content-section
    h2.content-section__title