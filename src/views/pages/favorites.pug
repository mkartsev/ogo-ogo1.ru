extends ../layouts/default

include ../../components/listing/listing

append vars
  -
    page.name  = "favorites"
    page.title = "Избранное"
    //- page.showHeader = true
    //- page.showTitle = true
    //- page.showBreadcrumbs = true

prepend scripts
  //- Скрипты только для этой страницы

block content
  section.content-section.mb-0.p-0
    .row.g-0
      .col.p-2.p-lg-3.bg-secondary
        +listing-controls({"filters": false, "pagination": false})
        +listing-grid(columns=4 id="listing-grid").listing-grid--view-grid